syntax = "proto3";

package akc;

service AuthorizedKeys {
    rpc Auth(User) returns(Keys) {}
}

message User {
    string username = 1;
}

message Keys{
    message Key {
        string key = 1;
    }
    
    repeated Key keys = 1;
}